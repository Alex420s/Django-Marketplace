{
    "builds": [
      {
        "src": "manage.py",
        "use": "@vercel/python",
        "config": {
          "installCommand": "pip install -r requirements.txt && npm install",
          "buildCommand": "npm run build && python manage.py collectstatic --noinput && python manage.py migrate  ",
          "startCommand": "gunicorn core.wsgi"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "manage.py"
      }
    ]
  }
  